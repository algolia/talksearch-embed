// Install WebPack
// Should package talksearch, as defined in ./src on top of all the search.js
// Should also include lodash if needed
// Should define talksearch as a global here
//
// Replace the js build step with one with webpack
// Each demo should have a packaged version
const search = talksearch({ indexName: 'takeoffconference' });
